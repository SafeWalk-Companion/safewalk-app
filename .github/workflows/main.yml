name: Check Title Format

on:
  pull_request:
    types: [opened, edited, synchronize, reopened]
  pull_request_target:
    types: [opened, edited, synchronize, reopened]

permissions:
  id-token: write # This is required for requesting the JWT (OIDC)
  contents: read # This is required for actions/checkout
  actions: read

jobs:
  build-backend:
    name: Build SafeWalk.WebAPI
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build SafeWalk.WebAPI
        uses: ./.github/workflows/actions/build-backend
        with:
          dotnet-version: '9.0.x'
          configuration: 'Release'
          solution-path: 'Backend/SafeWalk.WebAPI/SafeWalk.WebAPI.sln'

  test-backend:
    name: Test SafeWalk.WebAPI
    runs-on: ubuntu-latest
    needs: build-backend
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run XUnit Tests
        uses: ./.github/workflows/actions/test-backend
        with:
          dotnet-version: '9.0.x'
          configuration: 'Release'
          solution-path: 'Backend/SafeWalk.WebAPI/SafeWalk.WebAPI.sln'
          test-project-path: 'Backend/SafeWalk.WebAPI/SafeWalk.WebAPI.Core.Tests/SafeWalk.WebAPI.Core.Tests.csproj'

  check-pr-title:
    name: Check PR Title
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check PR Title Format
        uses: ./.github/workflows/actions/check-title
        with:
          title: ${{ github.event.pull_request.title }}

      - name: Comment on PR if invalid
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            const message = `## PR Title Format Check Failed
            
            Your PR title does not match the required conventional commit format.
            
            **Expected format:** \`<type>[(scope)]: [KAN-XXX] <description>\`
            
            **Valid types:** build, chore, ci, config, docs, feat, fix, perf, refactor, revert, style, test
            
            **Scope:** Optional, alphanumeric
            
            **Kanban reference:** Optional, format \`[KAN-XXX]\`
            
            Please update your PR title to match this format.`;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: message
            });
