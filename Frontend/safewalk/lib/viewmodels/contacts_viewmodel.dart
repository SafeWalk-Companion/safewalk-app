// ContactsViewModel manages state for the Contacts screen.
//
// Currently a placeholder — extend this ViewModel when contact management
// features (e.g. emergency contacts list) are implemented.

import 'package:flutter/foundation.dart';

class ContactsViewModel extends ChangeNotifier {
  // Placeholder – add contacts-specific state and logic here.
}
