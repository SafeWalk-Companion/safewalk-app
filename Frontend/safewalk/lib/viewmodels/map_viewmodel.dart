// MapViewModel manages state for the Map screen.
//
// Currently a placeholder — extend this ViewModel when map-related features
// (e.g. route tracking, nearby alerts) are implemented.

import 'package:flutter/foundation.dart';

class MapViewModel extends ChangeNotifier {
  // Placeholder – add map-specific state and logic here.
}
